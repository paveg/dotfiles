{
	"statusLine": {
		"type": "command",
		"command": "npx -y ccusage statusline", // Use "npx -y ccusage statusline" if you prefer npm
		"padding": 0 // Optional: set to 0 to let status line go to edge
	}
}
