# Alpine Linux packages (apk)
# Essential CLI tools only
curl
wget
git
zsh
tmux
build-base
fd
ripgrep
bat
jq
unzip
ncurses
github-cli
# Note: neovim is installed via dedicated script for latest version
# Note: exa replaced by eza (installed via cargo)
# Note: atuin is not available in Alpine repos, install via cargo or binary
# Note: gh (GitHub CLI) installed via apk if available, fallback to binary
# Note: mise is installed via dedicated script using official installer